<INSTRUCTIONS>
When the user says "gp", run:
1) git add .
2) git commit -m "<concise message you choose>"
3) git push origin main

When the user asks to "check screenshot(s)" or "look at screenshots", first check:
- /storage/emulated/0/DCIM/Screenshots
- /storage/emulated/0/Pictures/Screenshots

Default behavior:
- sort by most recent modified time
- inspect the latest 3 screenshots unless the user asks for a specific file
</INSTRUCTIONS>
